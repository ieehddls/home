<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="BaseballMapper">
  <select id="selectAll" resultType="ResultDTO">
    SELECT * FROM result
  </select>

      <!-- <select id="selectOne" parameterType="HashMap" resultType="ResultDTO">
      SELECT * FROM RESULT WHERE g_game_number = #{number} AND r_round = #{round}
    </select> -->

    <!-- <insert id="insertResult" parameterType="ResultDTO">
      INSERT INTO RESULT(g_game_number, r_round, r_answer_first, 
        r_answer_second, r_answer_third, r_hit) VALUES (#{g_game_number},
        #{r_round}, #{r_answer_first}, #{r_answer_second},
        #{r_answer_third}, #{r_hit})
    </insert>
    -->
</mapper>